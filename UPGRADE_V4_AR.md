# ترقية ELDEN AI إلى V4

## مهم قبل التثبيت
احتفظ فقط بنسخة من الملفين التاليين من مشروعك القديم:
- `.env` — إعدادات البوت والمفاتيح.
- `elden_ai.db` — الأعضاء والاشتراكات وحصص الاستخدام.

لا تنسخ `app/video.py` أو أي ملف Python قديم إلى النسخة الجديدة، لأن هذا يعيد أخطاء التعديلات اليدوية السابقة.

## خطوات الاستبدال على Windows
1. أوقف نافذة ELDEN AI القديمة كلياً.
2. فك ضغط النسخة الجديدة في مجلد جديد، مثلاً `D:\AGENTS\ELDEN-AI-V4`.
3. انسخ `.env` و`elden_ai.db` فقط من المجلد القديم إلى المجلد الجديد.
4. تأكد أن Agnes يعمل وأن هذا الرابط يرجع JSON: `http://127.0.0.1:8765/api/concurrency`.
5. شغل `start_windows.bat` من المجلد الجديد.
6. أرسل `/video` للبوت وجرب فيديو جديد.

## إعدادات Agnes المقترحة داخل `.env`
```env
AGNES_BASE_URL=http://127.0.0.1:8765
AGNES_POLL_SECONDS=10
AGNES_TASK_TIMEOUT=3600
AGNES_STATUS_TIMEOUT=90
AGNES_DOWNLOAD_TIMEOUT=120
AGNES_DOWNLOAD_RETRIES=12
TELEGRAM_VIDEO_MAX_MB=49
```

## كيف تم حل مشكلة التنزيل
- يقرأ البوت `final_video_file` من رد Agnes ويحاول نسخ الملف مباشرة إذا كان Agnes والبوت على نفس الجهاز.
- إذا لم يكن المسار قابلاً للوصول، يستعمل المسار الرسمي `/api/video/{task_id}`.
- يعيد محاولة تنزيل الملف إذا أكمل Agnes الحالة قبل أن يصبح `final_video.mp4` متاحاً.
- يكتب إلى ملف `.part` ثم يتحقق من الحجم وينقله بشكل ذري إلى `.mp4`.
- يعيد محاولة رفع Telegram ثلاث مرات ويستعمل الإرسال كوثيقة كخطة أخيرة.

## السجلات عند حدوث مشكلة
- `logs/startup.log`
- `logs/runtime.log`

لا تعدل `app/video.py` يدوياً. أرسل سجل الخطأ كاملاً فقط إذا ظهر خلل جديد من خدمة خارجية.
