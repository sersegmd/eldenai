services:
  searxng:
    image: searxng/searxng:latest
    container_name: elden-searxng
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:8081/
