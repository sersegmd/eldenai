# ELDEN AI 6.5 — OpenRouter + Live Web Research

- OpenRouter `thinkingmachines/inkling:free` with reasoning enabled.
- Preserves `reasoning_details` across session turns.
- Automatic DDGS metasearch for every conversational request.
- Injects source URLs as untrusted evidence and falls back gracefully if search is unavailable.
- Ollama/Puter are no longer used by the language routing path.
